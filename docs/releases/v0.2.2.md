# v0.2.2 发布验收记录

记录日期：2026-08-01

## 当前状态

- 发布候选版本：`versionName=0.2.2`、`versionCode=8`。
- 范围仅包含 Android 展示层、品牌资源、地图本地构建配置修复和无线路错误解释入口；后端、Room schema、路线算法、权限流程与 `NavigationService` 行为不变。
- 用户已完成当前 Debug 候选的真机人工验收，明确表示任务完成，随后授权提交、创建 PR 并发布稳定小版本。这是 v0.2.2 的用户界面验收门槛，不冒充正式签名 APK 真机覆盖证据。
- PR、标签、GitHub Release 和精确公开资产验证尚待本次发布流程完成；未完成项不得标记为通过。

## 发布前本地与真机证据

- [x] 127 个 JVM 测试 / 26 个套件通过，0 失败、0 错误、0 跳过。
- [x] `lintDebug`、`assembleDebug`、`assembleDebugAndroidTest` 与 AndroidTest Kotlin 编译通过。
- [x] Xiaomi 15T Pro（Android 16 / API 36）通过新增 7 个 Compose UI 契约测试和既有 10 个 instrumentation 测试。
- [x] 真机人工检查覆盖地图授权恢复、路线设置卡片垂直居中、无线路详情内容及 Google 地图公交交接。
- [x] 所有真机安装均先核对包名，再通过唯一无线 ADB transport 执行 `install -r`；未卸载、清数据或运行 connected Gradle 任务。
- [x] 最终 `testDebugUnitTest`、`lintDebug`、`lintRelease`、`assembleDebug`、正式签名 `assembleRelease` 和 `assembleDebugAndroidTest` 通过；Debug/Release Lint 均为 0 个问题。
- [x] 跟踪源码凭据审计、Navigation R8 反射审计和 Release APK 内容审计通过。
- [x] 后端 29 个测试、TypeScript 构建与生产依赖审计通过；0 失败、0 跳过、0 个生产依赖漏洞。

本地签名发布候选为 `app-release.apk`，49,102,773 字节，SHA-256 `17fdf79ed9789a6631430f9cbe329a4bd7b42a0840614479157705fa714d8bae`。`aapt` 核对为 `cn.anitabi.navigator` / `0.2.2 (8)` / minSdk 26 / targetSdk 37；`apksigner` 核对为单一 RSA-4096 签名者、APK Signature Scheme v2，证书 SHA-256 与既有正式版本一致。该哈希只属于最终本地候选，不能预先当作 GitHub Release 资产哈希。

## PR、CI 与 Release

- [ ] 发布 PR 的 Android、后端、API 26 与 API 37 检查全部通过并合并到 `main`。
- [ ] `v0.2.2` 标签指向 `main` 中已验证的发布提交。
- [ ] `Signed APK Release` 工作流成功，并由该工作流创建非草稿、非预发布的 Latest Release。
- [ ] Release 同时包含 `anitabi-v0.2.2.apk` 和 `anitabi-v0.2.2.apk.sha256`。
- [ ] 精确 Release APK 的 SHA-256 与校验资产、GitHub 资产摘要一致。
- [ ] APK 为 `cn.anitabi.navigator` / `0.2.2 (8)`，固定证书与 v0.2.1 相同，`apksigner verify` 通过。
- [ ] 精确 Release APK 在 API 26 与 API 37 完成下载、版本检查、安装、冷启动、首次导览和崩溃缓冲区验证。

## 证据边界

- 真机人工验收使用的是发布前 Debug 候选，不冒充精确公开 Release APK 覆盖安装证据。
- v0.2.2 的稳定发布边界经用户明确接受调整为：Debug 候选真机 UI 验收 + 本地/CI 固定签名与发布审计 + 发布后精确公开 APK 的 API 26/API 37 兼容门禁。没有执行的正式签名真机覆盖安装保持未验证，不会在本记录中补勾。
- Consumer Google Maps 能为某些端点找到公交路线，不代表 Google Routes API 对相同 raw 坐标一定返回路线；v0.2.2 只新增透明错误详情与外部核对入口。
- 本记录不得包含用户点位名称、实际坐标、Token、原始 IP、路线正文或其他敏感运行数据。
