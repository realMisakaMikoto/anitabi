# v0.2.1-rc.1 验收记录

状态：准备发布正式签名 GitHub Prerelease；稳定 v0.2.1 尚未发布。

## 已完成的候选门禁

- [x] 基线为公开 v0.2.0 与当前 main；`versionCode=7`、`versionName=0.2.1`。
- [x] Google Navigation SDK、Routes API、Firebase Anonymous Auth、Analytics 与 Crashlytics 接入完成；Maps SDK、MapLibre、OpenFreeMap、ORS 与 Transitous 当前路径已移除。
- [x] 自建 VPS、独立 DNS/HTTPS、Firebase JWT、服务账号 OAuth、SQLite WAL 配额、脱敏日志、七日备份和 fail-closed 恢复已部署验证。
- [x] 真实小额 WALK route、2×2 matrix 与 1 目的地 navigation reservation 通过；账本已保守对账。
- [x] 12 个并发 SQLite 连接证明 Matrix 月度 9,000 元素硬上限不会被突破。
- [x] 65 个 JVM 测试、Debug/Release Lint、Debug APK、androidTest APK、源码密钥审计和 APK 内容审计通过。
- [x] API 26/API 37 完成冷启动、真实首次权限导览、Room 1→2 迁移、遥测迁移、两次离线恢复、前台服务、息屏模拟位置自动到达和崩溃检查。
- [x] PR 1 的最终模拟器运行 `30530550542` 全绿：verify 5m46s、API 26 7m41s、API 37 7m18s。
- [ ] RC 发布工作流、固定签名、APK SHA-256 与精确 Release APK 模拟器复验（发布后填写）。

## 稳定版前仍必须完成

- [ ] Xiaomi 15T Pro 从公开 v0.2.0 覆盖安装 RC/最终 v0.2.1，不卸载、不清数据。
- [ ] 证明导览、多作品选择、顺序、设置与导航进度保留，旧 ORS Key/路线移除。
- [ ] 真机接受 Google Navigation 条款，验证 GMS 地图、原生道路语音、真实定位、锁屏、偏航、到达/停留/下一站。
- [ ] 验证长行程生产 20 目的地配额批次和后续批次原子预留。
- [ ] 验证公交相邻分段、无原生公交导航、无 Google 指令朗读，以及断网/VPS/额度错误恢复。

## 安全边界

- RC APK 不得包含服务端 Google 凭据、VPS 密码、签名密码、ORS Key、Transitous/ORS/OpenFreeMap/MapLibre 请求路径或 keystore。
- 本记录不得写入 Token、坐标、动漫名、搜索词、路线正文、IP、服务账号或任何密码。
- 用户已明确禁止当前任务在其手机上安装任何内容；本 RC 只允许在 GitHub 模拟器和之后获得明确许可的覆盖安装验收中使用。
