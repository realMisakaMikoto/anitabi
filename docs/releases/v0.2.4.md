# v0.2.4 发布验收记录

记录日期：2026-08-03

## 当前状态

- 发布候选版本：`versionName=0.2.4`、`versionCode=10`；Room schema 保持 2。
- 本版本新增首次导览的后台导航设置提示，并修复日本外部公交在用户静止于目标附近时可能无法完成到达候选计时的问题；后端协议、生产部署、Google 地图/Routes 参数、日本公交地区分流、到达半径与计时阈值、Room schema 和路线额度策略均未修改。
- 首次发布提交由 [PR #19](https://github.com/realMisakaMikoto/junrei_navi/pull/19) 合并；本次修复提交 `44c11da2f87bf46dfa899167931e514d605121a2` 由 [PR #21](https://github.com/realMisakaMikoto/junrei_navi/pull/21) rebase 合并为 main 提交 `dc7dafe59421bf348957df406cd77e2a14e5433c`。经用户明确授权，原 `v0.2.4` Release 与旧标签已删除并重建；当前标签固定指向该 main 提交，正式 [GitHub Release](https://github.com/realMisakaMikoto/junrei_navi/releases/tag/v0.2.4) 为 Latest、非草稿、非预发布。

## 自动验证与签名

- [x] 44 个 JVM 套件 / 246 个测试，0 失败、0 错误、0 跳过。
- [x] Debug/Release Lint 均为 0 个问题；Debug、AndroidTest、Release Kotlin 和正式签名 Release 构建成功。
- [x] tracked-source、Navigation R8 反射和 APK 内容审计通过。
- [x] 替换后的精确公开 APK 经独立包信息与 `apksigner` 核对为 `cn.anitabi.navigator` / `0.2.4 (10)`、minSdk 26、targetSdk 37、APK Signature Scheme v2、单一 RSA-4096 签名者和固定证书。
- [x] 后端 Node 24.14.0 类型检查、构建和 29/29 测试通过，生产依赖审计为 0 个漏洞。
- [x] 修复 PR 运行 [`30796654112`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30796654112) 与 main 运行 [`30797581053`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30797581053) 的 backend、verify、API 26、API 37 均成功。
- [x] 新 `v0.2.4` 标签触发的 Signed APK Release 运行 [`30798717699`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30798717699) 成功；公开资产兼容运行 [`30799365407`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30799365407) 的 API 26 与 API 37 均成功安装并冷启动正式 APK。
- [x] 替换后的公开 APK 与校验文件已从 Release 下载链接独立下载并核验：APK 共 49,227,274 字节，SHA-256 `a4fe6f075feee6f559935af7a8c668dbfdb7bdd1bc2412cee78b1c4ba5033068`；校验文件共 85 字节，SHA-256 `0cb0b05c7ebef4fbdeddeb2c79a66ef3b22bc0e774bc2977e51107f9cc77bd64`，正文、GitHub 资产摘要与实算值一致。公开 APK 包名/版本、SDK、v2 单一 RSA-4096 签名者、证书 SHA-256 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a` 及内容审计均通过。
- [x] Signed APK Release 曾记录一次对仓库 URL 的 GitHub 503 annotation；这是 GitHub 端瞬时访问失败，未中断 job。该 run 的测试、签名、R8、内容审计与发布步骤全部成功，Release 对象、标签、公开资产重下载和后续双 API 兼容门禁均复核通过，因此没有取消或重跑。

## 首次导览回归

- [x] 权限页显示电池优化、后台锁定和悬浮窗三个非阻断建议；保持原有圆角卡片、色彩、排版、单一主 CTA 和至少 48dp 操作高度。
- [x] 电池优化状态使用 `PowerManager.isIgnoringBatteryOptimizations()`，入口为通用 `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS`；没有新增直接白名单 manifest 权限。
- [x] 悬浮窗状态使用 `Settings.canDrawOverlays()`，入口为本应用 `ACTION_MANAGE_OVERLAY_PERMISSION`；返回前台后刷新状态。
- [x] 厂商后台锁定只提供诚实的手动说明，不伪造跨厂商 API 或已完成状态；设置页不可用时回退应用详情并显示内联错误。
- [x] API 26 与 API 37 的完整首次导览 instrumentation 已在 PR 与 main CI 实际执行并通过。

## 日本公交到达判定回归

- [x] `NavigationService` 仅对日本外部公交把定位订阅的最小位移从 2 米调整为 0 米，使静止用户仍能收到定时定位样本；道路导航和其他模式继续使用 2 米。
- [x] 到达状态机保持精度不差于 50 米、80 米内连续 15 秒进入 `ARRIVING`、超过 120 米重置；Google 地图 URL、Routes 请求、后端、持久化和状态半径均未修改。
- [x] 新增单元回归覆盖两类定位订阅策略；PR 与 main 的 API 26/API 37 runtime 均通过熄屏 mock GPS 自动到达测试。Android 官方 [`LocationManager`](https://developer.android.com/reference/android/location/LocationManager) 文档确认最小位移是定位更新之间的最小距离，Android 4.1 起兼容设备会同时遵守最小时间和最小距离。

## 升级、真机与边界

- [x] 用户在原公开 v0.2.4 个人真机上实测并提供截图，确认距目标 13 米、等待 30 秒后仍停留 `NAVIGATING`；该证据只用于复现修复前缺陷，不代表替换包已验证。
- [ ] 用户把手机带走并明确要求停止 ADB 后，未在个人真机安装或复测替换后的精确公开 APK；不得把 JVM、CI 或模拟器结果冒充真实静止定位闭环。
- [ ] 未在个人真机执行公开 v0.2.3 → 当前正式签名 v0.2.4 原位覆盖和 Room 2 数据保留验收。
- [x] v0.2.3 的日本/非日本/混合地区分流、Google 地图交接、悬浮控制和 ADB mock `ARRIVING` 历史真机证据保持原记录；本版本未改这些路径，但历史证据不冒充 v0.2.4 实体安装。
- [x] 未运行个人设备上的 `connectedAndroidTest`，未安装、卸载或清除设备应用与数据。

## 证据边界

- JVM、编译、Lint、静态审计和 API 26/API 37 模拟器不能替代厂商后台锁定、真实 Google 地图、通知、悬浮窗和真实定位的个人真机验收；本次替换后的真实静止到达仍待用户后续复测。
- 本记录只填写实际完成的提交、PR、CI、签名和公开资产结果；未完成项保持未勾选。
- 不记录 Token、密码、签名材料、服务器凭据、原始 IP、坐标、作品/搜索文本或路线正文。
