# v0.2.4 发布验收记录

记录日期：2026-08-03

## 当前状态

- 发布候选版本：`versionName=0.2.4`、`versionCode=10`；Room schema 保持 2。
- 本版本只新增首次导览的后台导航设置提示；后端协议、生产部署、Google 地图/Routes 参数、日本公交分流、导航状态机和路线额度策略均未修改。
- 发布提交 `a734543eaf8887f7ae843b2382622570398b2d9e` 已由 [PR #19](https://github.com/realMisakaMikoto/junrei_navi/pull/19) rebase 合并为 main 提交 `20d3ba00417743d77fda1be539350087d717032f`；`v0.2.4` 标签固定指向该 main 提交，正式 [GitHub Release](https://github.com/realMisakaMikoto/junrei_navi/releases/tag/v0.2.4) 已发布为 Latest、非草稿、非预发布。

## 自动验证与签名

- [x] 44 个 JVM 套件 / 245 个测试，0 失败、0 错误、0 跳过。
- [x] Debug/Release Lint 均为 0 个问题；Debug、AndroidTest 和正式签名 Release 构建成功。
- [x] tracked-source、Navigation R8 反射和 APK 内容审计通过。
- [x] 本地候选经独立包信息与 `apksigner` 核对为 `cn.anitabi.navigator` / `0.2.4 (10)`、minSdk 26、targetSdk 37、APK Signature Scheme v2、单一 RSA-4096 签名者和固定证书；共 49,227,286 字节，SHA-256 `9214df6c36cb3a972d228cd756bd11deafb189d508d0217d5c6002a11f6f3f56`。
- [x] 后端 Node 24.14.0 类型检查、构建和 29/29 测试通过，生产依赖审计为 0 个漏洞。
- [x] PR 运行 [`30782608175`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30782608175) 与 main 运行 [`30783228091`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30783228091) 的 backend、verify、API 26、API 37 均成功。
- [x] `v0.2.4` 标签触发的 Signed APK Release 运行 [`30784176972`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30784176972) 成功；公开资产兼容运行 [`30784613304`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30784613304) 的 API 26 与 API 37 均成功安装并冷启动正式 APK。
- [x] 公开 APK 与校验文件已从 Release 下载链接独立下载并核验：APK 共 49,227,274 字节，SHA-256 `9f894d44f22d372cd446a2004cce6949627a25f24b01d19d8124636bf940f199`；校验文件共 85 字节，SHA-256 `cb01091089426ee200758f895208f1fbe05c5043635ed3738e82657ca8e153fa`，正文、GitHub 资产摘要与实算值一致。公开 APK 包名/版本、SDK、v2 单一 RSA-4096 签名者、证书 SHA-256 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a` 及内容审计均通过。

## 首次导览回归

- [x] 权限页显示电池优化、后台锁定和悬浮窗三个非阻断建议；保持原有圆角卡片、色彩、排版、单一主 CTA 和至少 48dp 操作高度。
- [x] 电池优化状态使用 `PowerManager.isIgnoringBatteryOptimizations()`，入口为通用 `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS`；没有新增直接白名单 manifest 权限。
- [x] 悬浮窗状态使用 `Settings.canDrawOverlays()`，入口为本应用 `ACTION_MANAGE_OVERLAY_PERMISSION`；返回前台后刷新状态。
- [x] 厂商后台锁定只提供诚实的手动说明，不伪造跨厂商 API 或已完成状态；设置页不可用时回退应用详情并显示内联错误。
- [x] API 26 与 API 37 的完整首次导览 instrumentation 已在 PR 与 main CI 实际执行并通过。

## 升级、真机与边界

- [ ] 未在个人真机安装 v0.2.4 候选或公开 APK；没有当前任务授权时保持未勾选，不得冒充完成。
- [ ] 未在个人真机执行公开 v0.2.3 → 正式签名 v0.2.4 原位覆盖和 Room 2 数据保留验收。
- [x] v0.2.3 的日本/非日本/混合地区分流、Google 地图交接、悬浮控制和 ADB mock `ARRIVING` 历史真机证据保持原记录；本版本未改这些路径，但历史证据不冒充 v0.2.4 实体安装。
- [x] 未运行个人设备上的 `connectedAndroidTest`，未安装、卸载或清除设备应用与数据。

## 证据边界

- JVM、编译、Lint、静态审计和 API 26/API 37 模拟器不能替代厂商后台锁定、真实 Google 地图、通知、悬浮窗和真实定位的个人真机验收。
- 本记录只填写实际完成的提交、PR、CI、签名和公开资产结果；未完成项保持未勾选。
- 不记录 Token、密码、签名材料、服务器凭据、原始 IP、坐标、作品/搜索文本或路线正文。
