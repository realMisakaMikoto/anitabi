# v0.2.1-rc.2 验收记录

状态：等待正式签名 GitHub Prerelease、精确发布包复验和 Xiaomi 覆盖安装。

## 本候选修复

- [x] Anitabi 可选文本字段出现数字、布尔值、数组或对象时，不再拒绝整部作品的有效点位；必需 ID 和坐标仍严格校验。
- [x] Google 动态地图模块初始化失败时不再让应用崩溃；选点页自动切换到列表，其他地图区域保留明确提示和重试入口。
- [x] 进程恢复后保留缓存的多作品和点位选择，不额外发起网络请求。
- [x] 道路手动拖动只刷新受影响的相邻路线范围，单次请求继续遵守 12 个位置上限。
- [x] 公交详情保留 Google 提供的上下车站名和站数，不虚构站台字段。
- [x] 步行和骑行界面显示 Google 要求的测试版安全提示。

## 本地门禁

- [x] 69 个 JVM 测试通过，零失败、零错误、零跳过。
- [x] Debug/Release Kotlin 编译与 Lint 通过。
- [x] Debug APK 和 Debug androidTest APK 构建通过。
- [x] 源码密钥审计、Debug APK 内容审计和 `git diff --check` 通过。

## 发布与真机门禁

- [ ] 分支 CI 的 verify、API 26 和 API 37 作业通过。
- [ ] 合并后的 `main` CI 通过。
- [ ] 正式签名 `v0.2.1-rc.2` Prerelease 发布并通过签名、哈希和精确 APK 复验。
- [ ] Xiaomi 15T Pro 不卸载、不清数据覆盖安装 RC2，确认既有数据保留。
- [ ] 复测异常作品多选成功，且 Google 地图初始化失败时不再崩溃并可继续列表选点。

## 安全边界

- 不记录 Token、IP、坐标、动漫名、搜索词、路线正文、服务账号、签名材料、VPS 凭据或任何密码。
- RC2 必须沿用公开 v0.2.0/RC1 的固定 RSA-4096 签名；不得在手机上混装调试签名包。
