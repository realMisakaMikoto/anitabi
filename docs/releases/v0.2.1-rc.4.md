# v0.2.1-rc.4 验收记录

状态：分支/main/签名/公开 APK 兼容门禁通过；等待目标 Android 12 真机覆盖复验。

## 本候选修复

- [x] 在 Android 12 真机复现 RC3 地图崩溃，并由同一发布包 R8 映射还原到点位标记的 `BitmapDescriptorFactory.defaultMarker` 调用。
- [x] 地图点位、路线点位和当前位置不再依赖客户端位图标记工厂；使用 Google 默认标记，并以透明度和层级区分选中状态。
- [x] 点位标记、相机与监听器操作出现运行时异常时回退到现有列表，不再让地图错误终止应用。
- [x] 保留 RC3 的 Navigation SDK R8 类合并限制和反射注册器包访问修复。

## 本地门禁

- [x] 新增标记选项回归测试先因实现缺失而失败，修复后通过。
- [x] 71 个 JVM 测试、20 个测试套件通过，零失败、零错误、零跳过。
- [x] Debug/Release Lint、Debug APK 和 Debug androidTest APK 构建通过。
- [x] 正式版 R8 重新压缩成功；源码中无 `BitmapDescriptorFactory` 调用。
- [x] 源码密钥审计、Debug APK 内容审计和 `git diff --check` 通过。

## 发布与真机门禁

- [x] 分支 CI 的 verify、API 26 和 API 37 作业通过（`30550636244`）。
- [x] 合并后的 `main` CI 通过（`30551504222`）。
- [x] 正式签名 `v0.2.1-rc.4` Prerelease 发布；Release 工作流 `30552400597` 和精确公开 APK 的 API 26/API 37 兼容工作流 `30552936822` 通过。
- [ ] 在当前 Android 12 真机上覆盖安装 RC4，不清数据并保留已加载点位。
- [ ] 地图区域显示真实 Google 底图和点位，应用进程存活且崩溃缓冲区为空。
- [ ] 仅选择两个点，成功生成一条低计费道路路线并显示路线预览。

公开 APK 为 48,686,861 字节，SHA-256 为 `bbf724e24f2fee36efaf9e137a2711688b7d0d06351aaa7bddebce064ea39ca7`。公开 checksum、GitHub asset digest 与独立下载计算一致；包版本为 0.2.1（7），minSdk 26、targetSdk 37，APK Signature Scheme v2 有效且固定证书未变。

## 安全边界

- 不记录 Token、原始 IP、坐标、动漫名、搜索词、路线正文、API Key、服务账号、签名材料、VPS 凭据或密码。
- RC4 必须沿用公开 v0.2.0 到 RC3 的固定 RSA-4096 签名；不得用调试签名包替代真机候选验证。
- 真机验收只执行最小两点路线请求；不使用当前作品的全部点位制造批量计费请求。

许可证：GPL-3.0-or-later，并附 `LICENSE` 中仅针对 Google Navigation/Firebase SDK 的窄范围链接例外。第三方服务与数据署名见 `NOTICE.md`。
