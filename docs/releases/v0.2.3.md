# v0.2.3 发布验收记录

记录日期：2026-08-01—2026-08-02

## 当前状态

- 稳定版本：`versionName=0.2.3`、`versionCode=9`；Room schema 保持 2。日本公交主发布阶段未修改 VPS 或后端协议；2026-08-02 的同版本额度策略更新只调整后端配额/限速内部实现与 Android 批次、文案，公开端点、请求体以及旧 v0.2.3 客户端必需的响应字段保持兼容。
- [PR #13](https://github.com/realMisakaMikoto/junrei_navi/pull/13) 已通过 Chrome rebase 合并；悬浮控制与“提前离开”功能提交为 `ec7a28fec6e75c067906b0b08b1c05cf2b67e328`。移除个人请求限制的 [PR #14](https://github.com/realMisakaMikoto/junrei_navi/pull/14) 也已通过 Chrome rebase 合并，当前实现源码为 `main@774064fc51181c32ff639c32d356aa76e86b7ae2`；`v0.2.3` 标签仍保持在 `f173ebafef87c246de1e7d86e5e52979fdd18660`，没有移动或重写。
- PR CI run [`30743065945`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30743065945) 与 main CI run [`30743646492`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30743646492) 的 backend、verify、API 26、API 37 均全部成功。
- Signed APK Release run [`30744241032`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30744241032) 成功创建非草稿、非预发布的 [v0.2.3 Release](https://github.com/realMisakaMikoto/junrei_navi/releases/tag/v0.2.3)；公开资产兼容 run [`30744459795`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30744459795) 的 API 26/API 37 均成功。
- PR #14 合并后的 main CI run [`30750623097`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30750623097) 精确基于 `774064fc51181c32ff639c32d356aa76e86b7ae2`，backend、verify、API 26 和 API 37 四项均成功。
- 已完成精确公开 v0.2.2 → 正式签名 v0.2.3 原位覆盖、Room 2 数据保留、日本/混合/非日本核心功能以及新版悬浮控制的真机验收；用户确认核心功能无问题。新增“提前离开”经过本地与远端自动门禁，但未另行安装到个人真机。

## 已完成的本地证据

- [x] 主实现合并阶段的全量 `testDebugUnitTest` 共 43 个套件、231 个测试，0 失败、0 错误、0 跳过。覆盖持久化取消/提交后异常、缓存一致性、服务进度优先、活跃编辑 reload 超时/接管、进度合法推进、BOOT 策略隔离、暂停/终态 overlay 隐藏策略，以及实际 terminal writer 提交结果与旧 generation/plan/progress 拒绝条件；后续悬浮与提前离开修订的最终总数见下方记录。
- [x] `lintDebug`、`lintRelease` 均为 `No issues found`；`assembleDebug`、`assembleDebugAndroidTest` 和 AndroidTest Kotlin 编译通过。
- [x] 后端 TypeScript 类型检查与构建通过；29/29 测试通过；`npm audit --omit=dev` 为 0 个生产依赖漏洞。
- [x] v0.2.3 Release 代码完成 R8 收缩，新的 mapping/seeds/usage/configuration 已生成，Navigation SDK 反射审计通过。
- [x] Debug APK 内容审计及 tracked-source 凭据扫描通过；未读取 `.codex-remote-attachments/`。APK 为 `cn.anitabi.navigator` / `0.2.3 (9)`，共 78,581,900 字节，SHA-256 `702c2b8314a9792b8936ef70bed895ac59936a79287ec8f3e35fb4256b2518bc`；AndroidTest APK 为 1,170,750 字节，SHA-256 `44c38b01b6d84edd7204797955b74bc2465518da5564baed0d22e6068642d71b`。
- [x] APK 内 Natural Earth 日本资产为 159,056 字节，SHA-256 `f1cab5b0a94ef1873f9ee349652a80801ad9fc8c829e3c79970cf6752278cd8c`，与源码派生资产一致。
- [x] 使用工作区外既有 keystore 和当前 Windows 用户范围 DPAPI 保护的原密码，在单个 Gradle 子进程中临时注入四项 `ANITABI_*`；`assembleRelease` 串行构建成功，子进程退出时清除签名环境变量，未生成新 keystore，也未把密码或签名材料复制进仓库。
- [x] 修复前本地正式签名 Release APK 为 `cn.anitabi.navigator` / `0.2.3 (9)`，共 49,210,374 字节，SHA-256 `c48fdbbcf4ff90e4c97ea178ed0c7888297a895048b6047b6d791211083b9201`。`apksigner` 验证为 APK Signature Scheme v2、单一 RSA-4096 签名者，证书 SHA-256 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`，与 v0.2.2 固定签名完全一致；该文件只证明签名连续性，不得作为最终发布资产。
- [x] PR CI run [`30732960430`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30732960430) 基于 `e51f01499e3ee88349bda39bf8005800c9d94cda`：backend job `91456435038`、verify job `91456435019`、API 26 job `91457090492`、API 37 job `91457090499` 全部成功。前一 run `30732639827` 的 API 26 编译被取消，是 GitHub 延迟同步纯文档提交后由 `cancel-in-progress` 终止旧 run；新 run 两档模拟器均成功，排除代码失败。
- [x] 最终 PR CI run [`30733492538`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30733492538) 基于 `5a1216d840379253803cb5965c7a5736d91a12dd`：backend job `91457795058`、verify job `91457795057`、API 26 job `91458626011`、API 37 job `91458626032` 全部成功。main CI run [`30733984142`](https://github.com/realMisakaMikoto/junrei_navi/actions/runs/30733984142) 基于合并提交 `8ba07ade4e0b5437534a8d178f4a7bea81b02542`：backend job `91459164605`、verify job `91459164553`、API 26 job `91460029967`、API 37 job `91460029995` 全部成功。
- [x] 从 `main@8ba07ade` 执行 `:app:clean :app:testDebugUnitTest :app:lintRelease :app:assembleRelease --no-parallel --max-workers=1`，5 分 43 秒 `BUILD SUCCESSFUL`；随后 tracked-source、Navigation R8 与 APK 内容三项审计全部通过。最终本地候选为 `cn.anitabi.navigator` / `0.2.3 (9)`、minSdk 26、targetSdk 37，共 49,210,386 字节，SHA-256 `09c5fb5651f2c5c1e232790fbcbe7a01d6a6b532a0876300c1878896967aaaea`；`apksigner` 确认为 v2、单一 RSA-4096 签名者，固定证书 SHA-256 仍为 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`。
- [x] 通过已登录 GitHub 设置页只读核对发布工作流引用的 6 个 Actions Secret 名称均存在；未读取或输出 Secret 值。
- [x] 悬浮控制已改为单一可拖动/缩放面板和 60dp 可拖动悬浮球；默认 232×212dp、最小 216×188dp，四角停靠、横竖屏安全区、显示密度/字体变化、进程恢复、固定 48dp 主要操作及 TalkBack 替代入口均已实现。新增纯几何测试覆盖移动/缩放夹紧、四角折叠展开、四角尺寸往返、恢复、旋转与极小视口。
- [x] 悬浮修订、提前离开功能加入前的门禁为 44 个 JVM 套件 / 240 个测试，0 失败、0 错误、0 跳过；Debug/Release Lint 均为 `No issues found`，Debug/AndroidTest 构建、Release R8、tracked-source、Navigation 反射与 APK 内容审计全部通过。当时候选为 49,227,286 字节、SHA-256 `779ebeaef4ed397f328865e1d179bb23f1c6fd35a82ef3118b9fd43a655177ed`，仍为 v2、单一 RSA-4096 与固定证书。
- [x] 上述提前离开前候选已对真机现有 v0.2.3 执行 `adb install -r` 并保留数据；正式包仍为 versionCode 9，测试包保持原样且未卸载、清理或覆盖。冷启动恢复到本应用本身，没有自动打开 Google Maps，应用 crash buffer 为 0。
- [x] Xiaomi API 36 真机完成新版悬浮控制验收：恢复态 60dp 球为 195×195px；展开后的最小面板为 702×611px（216×188dp），轻触缩放柄切换到 754×689px（232×212dp），拖动缩放可增至 1050×937px。标题栏拖动只改变位置，连续 3 秒状态刷新不回跳；四角停靠、收球、跨屏拖球、轻触展开及收起前尺寸恢复均符合预期。截图只保存在系统临时目录，未写入仓库。
- [x] 横竖屏与进程恢复通过：1050×937px 面板在横屏重新夹紧到安全右上区域，恢复竖屏后回到原归一化位置，自动旋转和用户旋转设置均原样恢复。球停靠右下后强制停止正式进程，冷启动没有自动打开 Google Maps，也没有自动恢复 overlay；只点击“恢复行程”后，同一 60dp 球在原位置重现，展开仍保持原 dp 尺寸，仅有 1px 的密度舍入差。
- [x] 用户明确授权后使用平台 `cmd location`，短时把 `gps`/`network` 替换为测试 provider，并以 3 米精度在同一日本区域内注入两个均大于 120 米的样本。悬浮面板距离从 1.1 km 更新为 2.2 km，状态始终保持 `NAVIGATING` /“打开本段”，没有误进 `ARRIVING`；从悬浮面板点击“暂停”后 overlay 立即消失。`finally` 已移除两个测试 provider，shell mock app-op 恢复为 `default`，当前 `mMockProvider` 为 0，系统定位与旋转设置均恢复。
- [x] 真机收口时正式包仍为 `0.2.3 (9)`，测试包仍为原 `versionCode=0`；NavigationService 与 overlay 均未运行，页面保留暂停的日本公交测试行程。测试窗口内 `am_crash` / `am_anr`、crash buffer 和 Dropbox 应用 ANR 计数均为 0。既有 FakeGPS app-op 未改，但按用户要求改用 ADB 后其前台服务保持停止。
- [x] 真机报错闭环：首次 `appops get` 未限定用户而误扫到次用户并抛 `SecurityException`，改为先读取当前用户并显式传入 `--user` 后确认原始 shell mock 模式；首次 ADB 样本选在远离日本的区域，测试 provider 虽已移除，但应用进程保留最后一条 mock 定位，导致地图短暂跳到非洲。确认根因后立即停止正式进程、重新启用真实 provider、冷启动清除运行态，地图恢复日本行程范围；随后只使用同一日本区域内的有界样本重做并通过。首版清理断言还误把 location 事件历史当作当前 provider、且只匹配 `Default mode: deny`，改为核对实际 `MOCK_LOCATION: default`、当前 `mMockProvider=0` 与明确的 provider removal 事件后完成闭环。Git 上游只读查询和 Android 16 焦点查询也分别因无 upstream、旧字段缺失产生非修改性错误，改用 branch config 与 `topResumedActivity` 后确认状态。
- [x] 用户随后要求使用纯 ADB mock 触发 `ARRIVING`。在用户主动打开本段后，Google 地图保持前台；精度 3 米、目标 80 米内的连续样本维持超过 15 秒，悬浮面板显示直线距离 4 米、“已接近目标，请确认到达”和“确认到达”按钮。未点击确认到达；截图只保存在系统临时目录。脚本 `finally` 已移除 `gps`/`network` 测试 provider，shell mock app-op 恢复 `default`，当前 `mMockProvider=0`，正式/测试包版本未变，crash/ANR 计数为 0；用户之后明确表示测试通过并已停止当前行程。
- [x] 停留态新增“提前离开”：悬浮面板在禁用的“停留中”旁显示独立按钮，216dp 最小面板内两按钮各保持 48dp 高、间隔 8dp；应用内控制与通知提供同一入口。一次用户点击会通过既有可见 `TransitHandoffActivity` 和 repository CAS 原子结束停留并推进段号；中间站持久化后打开下一段 Google 地图，最后一站持久化为 `COMPLETED` 并关闭交接页，保存失败沿原路径回滚，不会后台自动打开地图或误开旧段。自然倒计时结束仍停在 `NEXT_STOP`，保持原手动开始规则。
- [x] 本次实现前复核了最新 Google 官方 [Maps URLs](https://developers.google.com/maps/documentation/urls/get-started) 与 Android 官方 [Activity lifecycle](https://developer.android.com/guide/components/activities/activity-lifecycle.html)、[后台 Activity 启动限制](https://developer.android.com/guide/components/activities/background-starts)。确认外部地图继续使用既有 `api=1` Directions URL，后台应用通常不得自行启动 Activity；因此只有用户点击“提前离开”后、交接 Activity 处于 resumed 状态时才打开下一段，服务、倒计时或恢复流程均不会自动拉起 Google 地图。
- [x] 提前离开修订后的最终本地门禁为 44 个 JVM 套件 / 243 个测试，0 失败、0 错误、0 跳过；新增覆盖中间站一键推进、末站完成、暂停 no-op、重复调用不跳段和返回起点策略。Debug/Release Lint 均为 0 问题，Debug、AndroidTest、Release Kotlin 与正式签名 Release R8 构建成功；tracked-source、Navigation R8 与 APK 内容审计通过。最终本地候选为 `cn.anitabi.navigator` / `0.2.3 (9)`、minSdk 26、targetSdk 37，共 49,227,286 字节、SHA-256 `67f2bd35e2aca560a57a3126bed9eb76cefb667fec2872b3e0771a5e24ed9a79`，并经 `apksigner` 确认为 v2、单一 RSA-4096 与固定证书；这是本地构建证据，公开 Release 资产的独立核对值见下方。
- [x] 正式候选首次重建在配置阶段报 `Release signing is not configured`，未生成新 APK。根因是构建脚本实际读取 `ANITABI_STORE_FILE`、`ANITABI_STORE_PASSWORD`、`ANITABI_KEY_ALIAS`、`ANITABI_KEY_PASSWORD`，而首次子进程误用了不存在的 `ANITABI_RELEASE_*` 名称；DPAPI 与 keystore 均未损坏。改为只读核对 `app/build.gradle.kts` 后使用准确变量名，并继续仅在单个子进程注入、`finally` 清除，重跑 5 分 28 秒 `BUILD SUCCESSFUL`；固定证书、R8 和三项审计均通过。
- [x] 悬浮控制与“提前离开”提交经 PR #13 和 `main` 两轮完整 GitHub 门禁；四个 job 在两轮中均成功，没有以旧 PR #12 的结果替代最终分支证据。
- [x] `v0.2.3` 标签精确指向 `f173ebafef87c246de1e7d86e5e52979fdd18660`。发布工作流完成源码凭据、标签版本、正式签名 Release、R8 反射、APK 内容、签名与校验文件检查并发布资产；随后两档公开资产兼容 job 均成功。
- [x] 从公开 Release 独立下载 `anitabi-v0.2.3.apk` 到随机系统临时单文件后重新核对：49,227,274 字节，SHA-256 `47374031a2836949179a9dc3c611e05e071bcb9a527f314253ad4a0d3a3e4a33`，包名 `cn.anitabi.navigator`、`0.2.3 (9)`、minSdk 26、targetSdk 37、APK Signature Scheme v2、单一 RSA-4096 签名者，证书 SHA-256 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`；APK 内容审计通过，临时文件已精确删除且无残留。公开校验资产 `anitabi-v0.2.3.apk.sha256` 为 85 字节，其 GitHub 资产摘要为 SHA-256 `ed6794708ea17808e5de92ed7bef9ecab9cdb1097713d92f5ac729100a7415c2`。

### 2026-08-02 同版本额度策略更新

- [x] 后端已移除 Matrix、Routes 与 Navigation 的 UID UTC 日额度以及 UID 突发令牌桶；Firebase Anonymous Token 验证继续强制执行，HMAC-IP 防滥用仍为容量 60、每秒恢复 5 次，共享 UTC 月度硬上限仍为 Matrix 9,000 元素、Route 9,000 次、Navigation 900 个目的地。SQLite 旧 `uid` 行不迁移、不读取、不写入；账本异常和费用开关关闭仍 fail closed。
- [x] `/v1/navigation/reserve` 继续为已发布旧 v0.2.3 客户端返回 `remainingToday`，固定兼容值为 `2147483647`；新 Android DTO 不再依赖该字段并继续忽略未知字段。道路导航生产批次从旧个人日额度对应的 20 恢复为 Navigation SDK 官方 25 目的地技术上限，61 点测试精确覆盖 25/25/11 三批。
- [x] 后端 TypeScript 类型检查、构建和 29/29 完整测试通过；测试覆盖旧 UID 行保持不变且不再参与限制、只写共享月记录、月切换、12 个独立 SQLite 连接不能突破全局上限、费用关闭 fail closed、HMAC-IP 独立桶及旧客户端响应兼容。`npm audit --omit=dev` 重试后确认生产依赖漏洞为 0。
- [x] Android 全量门禁为 44 个 JVM 套件 / 245 个测试，0 失败、0 错误、0 跳过；Debug/Release Lint 均为 0 问题，Debug、AndroidTest、Release Kotlin 与正式签名 Release R8 构建成功；tracked-source、Navigation R8 与 Debug/Release APK 内容审计全部通过。
- [x] 从精确合并源码 `main@774064fc51181c32ff639c32d356aa76e86b7ae2` 重新构建的本地正式候选为 `cn.anitabi.navigator` / `0.2.3 (9)`、minSdk 26、targetSdk 37，共 49,227,286 字节、SHA-256 `b5a125386a511151139f5ff69a4b88d7101af822b53d920db5f45a00aca75eec`；`apksigner` 确认为 v2、单一 RSA-4096 签名者和固定证书 SHA-256 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`。签名密码只在单个 Gradle 子进程中通过 Windows 用户范围 DPAPI 解密并注入，退出时清除；未生成新 keystore 或把签名材料复制进仓库。
- [x] 实现前查阅 Google 官方 [Routes API 用量与计费](https://developers.google.com/maps/documentation/routes/usage-and-billing)、[Navigation SDK Android 用量与计费](https://developers.google.com/maps/documentation/navigation/android-sdk/pricing)、[成本管理](https://developers.google.com/maps/billing-and-pricing/manage-costs) 与 [Routes 监控](https://developers.google.com/maps/documentation/routes/report-monitor)。确认 Routes 按请求、Matrix 按元素、Navigation 按目的地计费，Navigation 单次最多 25 个目的地；Google Cloud quota 可停止请求，而预算告警本身不会封顶费用。因此只移除项目内 UID 限制，继续保留共享月度硬上限、费用熔断、HMAC-IP 防滥用及 Google Cloud 配额。
- [ ] 发布顺序经独立兼容审计固定为“先部署新后端，再替换 v0.2.3 APK”：旧后端会拒绝新 APK 的 25 点批次，而新后端的兼容字段允许旧 APK 继续按原批次工作。PR 与 main CI 已完成；生产部署仍被服务器端 SSH 密码认证拒绝阻断，因此同版本公开资产尚未替换，本地候选不得冒充公开资产。

### 2026-08-02 生产部署尝试与安全回滚

- [x] 部署主机已经从 `main@774064fc` 的精确 Git archive 成功完成 Docker 构建；容器内 TypeScript 编译、生产依赖安装与审计均成功，补齐了本机 Docker Desktop 未完成的镜像构建门禁。
- [x] 一次切换尝试在备份成功、新容器健康及安全属性验证通过后，被部署后备份 systemd service 拒绝。根因是仓库中的 `backend/deploy/backup.sh` Git mode 为 `100644`，旧生产目录曾被人工保留为可执行，Git archive 却丢失该权限。部署器立即执行自动回滚，旧源码、旧镜像、容器、loopback 与公开健康检查全部恢复成功，SQLite 配额账本没有回滚。部署器已使用旧文件权限作为操作性兼容措施；本次后续提交把跟踪模式修正为 `100755`，防止未来 archive 再现。
- [x] 其他未进入切换的失败均未改变生产：候选 archive 的 Git blob 校验受跨主机换行影响，改为候选原始 SHA-256；生产工作树原始哈希同样受 CRLF 影响，改为生产 Git blob 语义校验；无 healthcheck 的无关容器不具备 `.State.Health`，改为稳定 inspect 后可空读取 Health。三项修正后均在进入下一门禁前验证，未删除或重启无关容器。
- [ ] 之后 Paramiko 与 Windows 系统 `ssh.exe` 都在远端脚本执行前被服务器端密码认证拒绝。一次性 OpenSSH 密码桥首版因随机令牌的二进制值与环境中的十六进制值比较不一致而返回空密码；修正后已用非敏感测试值验证逐字节完全一致，并在 15 分钟无连接冷却后只执行一次系统 SSH，服务器仍返回 `Permission denied (publickey,password)`。公共 API 同时持续返回 `service=ok`、`database=ok`，确认生产仍是回滚后的旧版本。安全恢复条件是现有 root 凭据重新被服务器接受；项目没有修改密码、SSH、端口、防火墙、主机密钥或登录策略。

## 未执行与边界

- [x] 本机 Docker Desktop 基础 Node 镜像拉取曾连续 20 分钟无层进度，遗留客户端已精确停止；部署主机随后从精确合并 archive 完成实际镜像构建。生产切换与最终健康验收仍受上方 SSH 认证阻断，不能把“镜像可构建”冒充“已经部署”。
- [ ] 未在个人真机运行 `connectedAndroidTest`；项目约束禁止该任务自动卸载正式包，现有测试包保持未动。
- [ ] 新增“提前离开”提交未另行安装到个人真机；此前正式签名候选上的地区分流、Google 地图交接、悬浮控制、恢复和 ADB mock `ARRIVING` 真机证据仍有效，但不冒充该按钮的实体点击验收。

## 证据边界

- JVM、编译、Lint 和静态 APK 审计不能替代真机上的 Google 地图、通知、悬浮窗、定位和后台行为验收。
- `assembleDebugAndroidTest` 只证明测试 APK 可构建，不代表 instrumentation 已执行；当前唯一连接设备为个人真机，依照项目约束未运行 connected Gradle 任务或替换测试包。
- PR 的 API 26/API 37 模拟器门禁不能替代实体设备上的真实 Google 地图、系统悬浮窗/通知权限与真实定位验收。
- 本地签名 APK 与公开 Release APK 是两次独立构建，字节数和哈希不同属正常；公开资产数据只采用发布完成后重新下载并独立核对的结果。
- 本记录只填写已经实际完成的公开 Release、CI、PR 或真机结果，不包含坐标、Token、服务器凭据、原始 IP 或路线正文。
