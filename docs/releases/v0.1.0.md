# v0.1.0 发布验收记录

核对日期：2026-07-29

结论：道路版已完成自动构建、静态验证、签名发布和 API 36 模拟器冷启动；完整计划尚未通过实体手机验收。Transitous 公交因未取得维护者明确同意而保持编译期关闭。

## 已通过并有直接证据

- [x] 公开仓库采用 GPL-3.0-or-later，`LICENSE`、`NOTICE.md`、README 和应用内关于页一致。
- [x] OpenFreeMap、OpenMapTiles、OpenStreetMap、ORS/HeiGIT、Transitous 数据来源和 Anitabi 署名已在对应界面保留。
- [x] OpenFreeMap、ORS 和 Transitous 政策于 2026-07-29 从官方页面重新核对；Transitous 未获同意，因此未启用。
- [x] GitHub Actions `30435837520` 完成单测、release Lint、R8、固定签名校验、SHA-256 生成和 Release 发布。
- [x] `anitabi-v0.1.0.apk` 大小为 50,984,377 字节，SHA-256 为 `e4c1c66cefaea54fde0c7f3f6bfbba461530465e5ef4530c02fa956bcd12624d`。
- [x] APK Signature Scheme v2 校验通过，单一签名者证书 SHA-256 为 `9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`。
- [x] 下载后的正式 APK 静态扫描未发现旧 ORS 域名、Organic Maps、Google Billing、Firebase/Analytics、签名密码变量、私钥标记或 keystore 文件；能找到预期的 Bangumi、Anitabi、HeiGIT 和 OpenFreeMap 地址。
- [x] GitHub Actions `30437176966` 完成 SDK 37 单测、Lint、debug APK 构建及 API 36 模拟器安装与冷启动；启动状态为 `ok`，应用进程存活，崩溃缓冲为空，中文首页截图正常。
- [x] 仓库公开，APK 通过免费的 GitHub Release 分发，没有应用商店、后端服务器、付费 SDK、Google Billing 或项目共享 API Key。
- [x] Bangumi 搜索和 OpenFreeMap 样式实时探针返回 HTTP 200；Anitabi 官方 API 在同一环境被 Cloudflare 403 拦截，已提交上游问题 [#86](https://github.com/anitabi/anitabi.cn-document/issues/86)，未把该项误记为通过。

## 自动化补强状态

- [ ] Android 8（API 26）与 Android 17（API 37）安装/冷启动矩阵通过。工作流已加入，等待主分支 Actions 结果后回填运行编号。
- [ ] 新增 APK 内容审计步骤在主分支 CI 通过。工作流已加入，等待 Actions 结果后回填运行编号。

## 仍需实体手机或外部授权

- [ ] 从搜索动漫、区域批选、输入用户自己的 ORS Key、生成路线到开始一次连续导航的真实端到端流程。
- [ ] 驾车、骑行、步行的真实 GPS、中文 TTS、自动到达、停留、下一站和偏航重算。
- [ ] 弱网/断网、GPS 跳点、拒绝权限、锁屏、OEM 后台限制、杀进程、重启恢复和跨午夜行为。
- [ ] Anitabi 上游确认 Cloudflare 403 的原因或给出原生 Android 客户端可用的访问要求；主分支已改为明确提示服务拒绝访问，但 v0.1.0 APK 尚未包含该提示修复。
- [ ] Transitous 维护者明确同意。未获同意前不运行东京公交、无覆盖地区或公交异常场景，也不设置 `ANITABI_TRANSITOUS_APPROVED=true`。

上述未完成项不得由单元测试、模拟器首页截图或源码存在性替代为“已通过”。
