# 巡礼手帖 v0.2.1 RC7

这是 v0.2.1 的正式签名候选版。用户已在真机确认 Google 底图与巡礼点、步行和驾车路线可正常使用；后来提供的复测证据表明公交仍会在部分相邻地点之间报告没有路线，因此公交验收已撤回。当前仍标记为预发布版本，稳定版 `v0.2.1` 尚未发布。

## 安装与升级

- 支持 Android 8.0 及以上版本。
- 从 v0.2.0 更新时请直接覆盖安装，不要卸载或清除应用数据。
- 导览状态、作品与点位选择、行程顺序、设置和导航进度会保留。
- 本机保存的旧 ORS Key 和旧路线缓存会被移除；新路线需要联网刷新。

## 主要变化

- 地图、定位以及步行、骑行、驾车道路导航迁移到 Google Navigation SDK。
- 路线预览和公交规划迁移到 Google Routes，并通过项目自建服务保护服务器凭据和共享额度。
- 用户不再需要申请或填写 ORS Key，也不需要注册邮箱账号或提供姓名。
- 可以同时选择多部动画，并把所有巡礼点显示在同一张地图上。
- 行程总点数不再有固定的 8 点或 12 点上限；应用会自动排序和分批规划。
- 公交按相邻地点逐段规划，显示线路、站点、换乘、时间和步行接驳。
- 修复此前候选版正式签名构建中的地图初始化与渲染崩溃、合法驾车响应被误判失败，以及公交时间参数被拒绝的问题。

## 使用前需要知道

- 地图和新路线需要网络，以及设备上可用的 Google Play 服务。
- 没有 Google Play 服务时，只能查看已经保存的点位、顺序和进度，没有备用地图或路线服务。
- 步行和骑行路线会显示 Google 要求的测试版提示；请以现场道路、交通规则和安全状况为准。
- 公交不会启动 Google 原生公交导航，站台信息只显示上游实际提供的文字。
- “总点数不限”只表示保存与规划没有固定产品上限。个人每日额度、项目共享额度和设备资源仍会限制当天可刷新的路线；原生导航当前每个匿名用户每天最多预留 20 个目的地。额度耗尽或服务维护时，本地行程与进度不会被删除。

## RC7 已知问题

- 道路导航按批次预留目的地。18 段行程会一次预留 18 个；若同一匿名用户当天此前已经预留 3 个或更多，启动会被 20 个目的地的个人日上限拒绝。RC7 会显示未翻译的英文配额信息，而且界面可能仍保留“正在引导”的旧状态；此时 Google 原生导航并未开始。
- 公交按相邻地点逐段请求。任一相邻段没有返回公交路线时，RC7 会中止整条行程并误显示“本区域暂无开放公交数据”。该结果只针对当前起终点和时间，不能证明整个区域没有公交数据；公交尚未通过真机验收。

## 隐私

- Firebase 匿名身份只用于访问路线服务和计算共享额度。
- Google 路线、折线、步骤、预计时间和公交详情只保存在当前运行内存中。
- Firebase Analytics 和 Crashlytics 默认关闭，只有用户分别主动同意后才会启用，也可以随时撤回。
- 项目日志不会记录 Token、原始 IP、坐标、动画名称、搜索词或路线正文。

完整说明见 [隐私政策](https://github.com/realMisakaMikoto/anitabi/blob/main/PRIVACY.md)。

## 文件校验

- APK：`anitabi-v0.2.1-rc.7.apk`
- 大小：48,703,245 字节
- SHA-256：`f4a1b53b20baddb16602488242cd71dace56b98a0a04cb5ee28ad214c08c3a1d`
- 签名证书 SHA-256：`9679c83769368c7150f629d9cba3c0e5d633fa7f1043ce251fdba6c7c64fb00a`

Release 同时提供 `anitabi-v0.2.1-rc.7.apk.sha256`。详细验收记录见 [`docs/releases/v0.2.1-rc.7.md`](https://github.com/realMisakaMikoto/anitabi/blob/main/docs/releases/v0.2.1-rc.7.md)。
