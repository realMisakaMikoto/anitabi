# v0.1.3 真机验收记录

日期：2026-07-29  
设备：Xiaomi 15T Pro（2506BPN68G）  
系统：Android 16 / API 36，安全补丁 2026-06-01  
应用：`cn.anitabi.navigator` v0.1.3（versionCode 4）

## 结论

签名版 v0.1.3 已在真机完成核心链路测试：冷启动、Bangumi 搜索、Anitabi 点位、Transitous 公交规划、连续导航、权限、前台服务、通知、后台返回、短时锁屏、断网保持和网络恢复均通过，Android 崩溃缓冲区始终为空。

> 2026-07-29 更正：本次验收把首屏判断为“无明显截断”是错误的。用户随后指出并经截图复核确认，应用顶部栏与系统状态栏重叠，底部操作区也侵入系统手势栏；同时，本文记录的公交 `0 m` 和 MapLibre 无效几何也是实际缺陷，不能仅归类为外部数据观察项。这三项已在 v0.1.4 源码中修复并重新真机验收。本文其余内容继续作为 v0.1.3 的历史记录。

仍有三项不能据此判定为最终通过：

1. 手机当前启用了允许模拟位置的应用，本次定位结果被 Android 标记为 mock，不能冒充真实 GNSS 实走。
2. 手机、项目配置和环境变量中都没有用户自己的 ORS Key，因此驾驶/骑行/步行模式只验证了空 Key 的本地拦截，没有向 ORS 发送真实路线请求。
3. Google TTS 已实际收到中文合成请求并完成 dispatch，TTS 音量流也未静音；但 ADB 无法代替人耳确认扬声器是否真的可听。

## APK 与启动

- 手机内 APK 与 GitHub Release `anitabi-v0.1.3.apk` 的大小均为 43,087,229 字节。
- 两者 SHA-256 均为 `c86d2b44db5c95f0518b5d876f3f9d7e7baac69ebc179472341fe12a97cd532b`。
- 冷启动结果为 `Status: ok`、`LaunchState: COLD`，总耗时 332 ms。
- `MainActivity` 成为 top-resumed Activity，进程存在，crash buffer 为空。
- 1280×2772、520 dpi 首屏中文可渲染，但顶部栏实际与系统状态栏重叠；此前“无明显截断”的判断已撤回。

## 在线数据与公交路线

- 使用 `Your Name` 搜索 Bangumi，返回 20 条结果并成功加载封面；正确打开 Bangumi `#160209`《你的名字。》。
- 在原 Wi-Fi 下，Anitabi 正确显示“当前公网 IP 被公共服务拒绝，请更换网络后重试”，没有无限重试或崩溃。
- 临时切换到移动数据后，Anitabi 成功加载 68 个可用点、地图瓦片、聚合标记和点位图片；测试结束后 Wi-Fi 与移动数据均恢复为开启。
- 选择两个距离过近的点时，公交规划返回“本区域暂无开放公交数据”，应用保持可操作。
- 改为“マンション桂”到“LABI新宿東口館前”后，Transitous 成功返回 3 段 WALK/公交行程，路线预览、站点、时刻和“开始连续导航”均可用。
- 观察到 MapLibre `Invalid geometry in line layer` 警告且公交段显示 `0 m`。后续对正式 Transitous 响应的复核确认：部分路段不返回 `distance`，客户端必须从 polyline6 几何计算距离，并过滤不足两个坐标的线；该缺陷已纳入 v0.1.4 修复。

## 连续导航与系统集成

- 启动导航后，精确/粗略定位和通知权限均为 granted。
- `NavigationService` 以 location 类型前台服务运行，`foregroundId=1001`。
- 持续通知标题使用当时的应用名加“· 连续导航”，正文为当前指令，包含“结束”操作，并带 `ONGOING_EVENT`、`NO_CLEAR` 和 `FOREGROUND_SERVICE` 标志。
- 定位请求使用 GPS 高精度与网络平衡模式，间隔 2 秒、最小位移 2 米。
- Google TTS 成功绑定并收到 `zho-CHN` 合成请求，dispatch 到中文 voice；`STREAM_TTS` 未静音并输出到扬声器。
- 退到桌面 4 秒后，进程、前台服务和通知保持；重新打开为 99 ms 热启动，路线和当前分段未丢失。
- 同时关闭 Wi-Fi 与移动数据 7 秒时，导航界面、缓存地图和分段指令保持，前台服务继续运行；恢复移动数据后默认网络自动重建。
- 小米息屏/Dozing 状态下，PID、前台服务和通知均保持，crash buffer 为空。
- 通过应用界面结束导航后，服务、持续通知以及 GPS/网络定位注册均被移除。
- 服务未导出，shell 尝试直接发送内部 STOP action 被 Android 拒绝，符合安全预期。

## 公路模式边界

- 驾驶模式在 ORS Key 为空时显示“请先填写自己的免费 ORS Key”。
- 没有发现环境变量、项目本地配置或手机应用状态中已有可用 Key。
- 未使用共享、伪造或第三方 Key，也未发送无凭据 ORS 请求。

## 尚需用户完成

- 在开发者选项中关闭模拟位置应用，处于能获得真实定位的位置后，实走一段路线并确认定位不是 mock。
- 在手机上自行粘贴个人 ORS Key，再分别生成驾驶、骑行或步行路线；不要在聊天或日志中发送 Key。
- 听一次中文导航播报，确认音量、发音与耳机/扬声器路由符合预期。
- 若要覆盖最初计划的完整现场验收，再进行一次 8–12 点实走、长时间 OEM 后台/省电限制和真实错过班次场景。
