# v0.1.4 真机验收记录

日期：2026-07-29（设备时区已进入 2026-07-30）

设备：Xiaomi 15T Pro（2506BPN68G）

系统：Android 16 / API 36，安全补丁 2026-06-01

应用：`cn.anitabi.navigator` v0.1.4 debug 候选完成完整流程；公开固定签名 v0.1.4（versionCode 5）完成产物一致性与冷启动复验

## 结论

用户指出的顶部状态栏遮挡已复现、修复并逐页真机复核；同时修复了底部手势栏遮挡、Anitabi 正式数据解析失败、Transitous 公交段 `0 m`、退化路线几何和公交时区显示。真实 ORS 与 Transitous 路线均成功生成，应用崩溃缓冲区为空；用户随后明确确认能听到本次真机导航的 TTS。

## 系统栏与页面布局

- 搜索首页、点位选择、规划设置、路线预览、连续导航和关于页的顶部标题/内容均位于状态栏下方。
- 全应用底部内容统一避开系统导航栏；规划页“生成完整路线”、预览页“开始连续导航”和导航页操作按钮均位于手势条上方。
- 验证分辨率为 1280×2772；状态栏与底部手势导航均保持开启，没有通过隐藏系统栏规避问题。

## 正式数据兼容

- 在移动数据出口访问正式 Anitabi API，Bangumi `#328609` 成功加载 74 个有效点位。
- 首次失败被定位到真实响应中的 `ep: "CD"`；`ep` 和 `s` 未参与产品逻辑，因此客户端改为忽略这些类型不稳定的字段，而不是强制转换或丢弃整个响应。
- 应用显示“数据可能不完整”并保持可选点、地图与图片可用。

## ORS 道路路线

- 用户自己的 ORS Key 通过应用输入并由 Android Keystore 加密保存；没有写入源码、测试、日志、截图、文档或 Git。
- 真机通过新的 `https://api.heigit.org/openrouteservice/v2/` 端点生成两站道路路线，结果为约 1 分钟、0.8 km，地图几何正常显示。
- 启动连续导航后，顶部标题和底部结束/到达操作均避开系统栏；结束后 `NavigationService` 与持续通知均不存在。

## Transitous 公交路线

- 正式 Transitous 返回金泽八景至下北泽的 7 段行程，总计 1 小时 44 分、47.2 km，地图显示完整路线。
- 实际铁路段常不包含 `distance`；修复后从 polyline6 坐标计算距离并同步到摘要。可见路段包括 24.3 km、2.5 km、429 m、231 m、354 m 和 781 m，没有有效路段显示 `0 m`。
- 正式响应的 UTC 时间结合站点 `Asia/Tokyo` 时区后正确显示为当地时间，例如末两段 `06:22 → 06:26`、`06:26 → 06:40`。
- 连续重复坐标被过滤，少于两个不同坐标的退化几何不再建立 LineString；本轮日志中没有 `Invalid geometry in line layer`。

## 设备状态恢复与证据边界

- 测试结束后 Wi‑Fi 已重新启用并连接，移动数据保持启用；没有残留导航前台服务或持续通知。
- 为安装 debug 候选包，已卸载 v0.1.3 签名包，因此原应用本地数据被 Android 清除。发布后又卸载 debug 包并安装公开 `anitabi-v0.1.4.apk`，Android 报告 `versionCode=5` / `versionName=0.1.4`；手机内 `base.apk` 与 Release 资产均为 43,103,613 字节，SHA-256 均为 `4a95482bdc9bdec9e357d334339f9a401f558b00f19b4160b519ea9af586240e`。
- 公开签名版真机冷启动为 `Status: ok` / `LaunchState: COLD`，总耗时 633 ms；首屏状态栏避让正确，应用崩溃缓冲为空，导航服务和持续通知均不存在。
- 卸载 debug 包同时清除了其测试路线和 Android Keystore 中的旧 ORS Key；正式版当前没有 Key，用户应撤销聊天中暴露过的 Key、生成新 Key 后再在应用内输入。
- 手机仍配置了 mock-location 应用，本轮不能冒充真实 GNSS 实走；用户已确认能听到中文 TTS，但未对发音质量作额外验收；长时间 OEM 省电限制和真实错过班次仍需用户现场完成。
- 聊天中暴露过的 ORS Key 必须由所有者撤销并重新生成；本文和仓库不记录其内容。
