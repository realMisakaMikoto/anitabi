# 巡礼手帳 v0.2.4 隐私说明

最后更新：2026-08-03

## 本机保存的数据

应用只持久化用户拥有或明确选择的数据：首次导览状态、多作品选择、Anitabi 巡礼点、手动顺序、起终点、出行模式、停留设置、执行策略、当前分段、暂停时间、已完成点和导航状态。应用禁止 Android 系统备份和设备迁移。

Google 路线矩阵、路线、折线、步骤、预计时间和公交详情仅保存在当前进程内存中，不写入 Room、SharedPreferences、文件或备份。v0.2.0 升级时会移除旧 ORS Key 密文、IV 和旧路线内容；无法迁移的原记录会保留并向用户显示恢复错误，不会静默清库。

## 路线请求

规划或刷新路线时，应用会将完成请求所需的坐标、出行模式、优化目标、出发或到达时间以及公交偏好经 HTTPS 发送到项目自建 API。Android 使用 Firebase Anonymous Auth 获取匿名 ID Token，只用于验证对项目路线服务的访问；不需要提供邮箱、姓名或密码。

自建 API 验证匿名 Token 后，通过服务器端 Google 服务账号调用固定的 Google Routes 上游。服务账号私钥不会进入 APK。服务器不缓存 Google 矩阵、路线、折线、步骤或公交正文。

若用户选择的公交巡礼点全部位于日本，应用会在发送任何路线请求前用随 APK 固定携带的 Natural Earth v5.1.1 日本边界离线分类。该分类不使用当前位置、作品城市名、Geocoder、时区或网络服务。日本模式的排序、直线距离和占位分段完全在本机生成，不向项目 VPS、Google Routes API 或其他地区服务发送规划请求。

服务器的新配额预留只按计费类别和 UTC 月度周期记录项目共享的已预留数量，不按匿名 UID 设置每日额度。现有账本及其备份可能继续保留旧版本写入的 UID 用量行，但新服务不会读取或写入这些行，也不会据此限制请求。Firebase 匿名 UID 不作为配额或突发限速键；防滥用限速只在内存中使用由服务器秘密生成的 HMAC-IP，原始 IP 不写入配额账本或日志。应用与服务器日志不得包含 Token、原始 IP、坐标、动漫名、搜索词、请求正文或 Google 响应正文。

## Google Navigation 与 Firebase

驾车、骑行和步行道路导航由设备上的 Google Navigation SDK 提供。Google SDK 可能根据其适用条款处理设备位置和导航交互；项目无法替代 Google 的隐私政策。

当用户明确点击日本公交的“开始日本公交行程”“打开本段”“开始下一段”，或在无线路详情中点击“在 Google 地图中查看路线”时，应用会通过 Android `ACTION_VIEW` 把该段精确起点、终点和公共交通模式交给已安装的 Google 地图；若未安装 Google 地图，则交给用户设备可处理同一 HTTPS 链接的应用。链接不含 API Key、途经点、时间或自动导航参数。该操作只在用户点击后发生；应用不读取 Google 地图的路线、班次、选择或导航状态，也不推断 Google 地图是否找到公交。

日本公交运行时使用 `location` 类型前台服务进行到达提示。首次交接要求 30 秒内且精度不差于 100 米的精确定位；接近判定只使用精度不差于 50 米的定位样本。用户可以选择 Android 悬浮窗作为外部地图期间的控制入口；未授权时使用可见的前台服务通知。悬浮窗权限、通知权限、全局通知开关和导航频道只在本机检查。两种控制入口均不可用时不会打开外部地图。

设备重启后，应用只在本机存在活动行程 ID 且通知可见时发布“行程可恢复”通知；不会自动启动定位、Activity 或 Google 地图。若重启后只有悬浮窗权限而通知不可见，用户需要手动打开应用恢复控制。

Firebase Analytics 与 Firebase Crashlytics 为两个独立、可选的开关：

- 两项默认关闭，只有用户明确选择加入后才启用。
- Analytics 只允许版本、设备能力、模式、点数区间、延迟区间和错误类型；不允许坐标、动漫名、搜索词、路线正文或用户 ID。
- 首次启用 Analytics 前会清理同意前的本机分析状态；撤回后关闭收集并重置本机分析数据。
- 首次启用 Crashlytics 前会删除同意前产生的未发送报告；撤回后立即删除未发送报告，并在下次启动完全停止收集。

用户可随时在“关于、隐私与数据来源”页面分别撤回两项同意。

## Bangumi 与 Anitabi

动漫搜索直接访问 Bangumi API。巡礼点 JSON 和图片由 Android 以可识别的 `AnitabiNavigator/0.2.4` User-Agent、按用户操作低频访问 Anitabi 官方 API 与图片域名，不经过项目 VPS。应用不抓取 Anitabi 主站，也不打包或批量下载全站数据。

## 保留、故障与联系

自建 API 的 SQLite 共享月度配额账本与审计状态位于项目 VPS，并执行七日一致性备份。若恢复后无法证明额度没有回退，计费请求保持关闭。路线内容不进入这些备份。

断网、VPS 故障或费用熔断时，已保存的行程和进度仍留在设备上，路线暂时无法刷新。卸载应用会按 Android 行为删除其本机私有数据。

问题与安全报告方式见 [SECURITY.md](SECURITY.md)，项目联系人为 [realMisakaMikoto](https://github.com/realMisakaMikoto)。Google 与 Firebase 的数据处理还受其各自服务条款和隐私政策约束。
